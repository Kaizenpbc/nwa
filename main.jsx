import React from "react";
import ReactDOM from "react-dom/client";
import NWAPrototype from "./nwa_prototype.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NWAPrototype />
  </React.StrictMode>
);
